# Docker example

This folder shows a minimal Docker Compose deployment for Winmem.

In the main repo, use:
- `infra/docker/docker-compose.dev.yml` for development
- `infra/docker/docker-compose.prod.yml` for production

This example is intentionally small and easy to read.
