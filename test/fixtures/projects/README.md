# Project fixtures

These project configs are used for tests that expect a stable set of parsing targets.
They should mirror the shape of `examples/configs/projects/*.yaml`, but are purpose-built
for deterministic tests.
