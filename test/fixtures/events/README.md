# Event fixtures

Events are Winmem's normalized, canonical objects produced by parsers + normalizers.
They are used to validate:
- canonical JSON serialization
- hashing
- merkle batching
- regression golden outputs
